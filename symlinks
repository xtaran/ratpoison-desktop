~/.gitconfig 				   → gitconfig
~/.gitignore 				   → gitignore
~/.xsession 				   → xsession
~/.screenrc 				   → screenrc
~/.colordiffrc 				   → colordiffrc
~/.emacs 				   → emacs.el
~/.lintianrc 				   → lintianrc
~/.xsettingsd 				   → xsettingsd
